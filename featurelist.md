# فهرست ویژگی‌های پلتفرم هوش مصنوعی (MyAI Platform)

این سند دربردارنده تمام قابلیت‌ها و ویژگی‌های کلیدی است که تا به امروز در این پلتفرم سازمانی هوش مصنوعی پیاده‌سازی شده‌اند. معماری این سیستم بر پایه معماری ماژولار (FastAPI برای بک‌اند، React/Vite برای فرانت‌اند) و دیتابیس قدرتمند (PostgreSQL با قابلیت pgvector) بنا شده است.

---

## ۱. مدیریت کاربران و سطوح دسترسی (User Management & RBAC)
*   **احراز هویت امن (JWT)**: سیستم ورود با استفاده از توکن‌های امن و رمزنگاری رمزهای عبور.
*   **سطوح دسترسی سه‌گانه (Roles)**:
    *   **ادمین سیستم (System Admin)**: دسترسی کامل به پنل مدیریت کاربران (ساخت، ویرایش، حذف و غیرفعال‌سازی کاربران)، مشاهده تمامی فضاهای کاری و مدیریت کل سیستم.
    *   **مدیر فضای کاری (Workspace Manager)**: قابلیت ساخت فضای کاری، تغییر تنظیمات فضای کاری خود و افزودن/حذف کاربران (فقط خواندن لیست کاربران برای افزودن آن‌ها به فضای کاری).
    *   **کاربر عادی (User)**: دسترسی فقط-خواندن به فضاهای کاری که در آن عضو شده است و قابلیت چت و پرسش‌وپاسخ.
*   **پنل مدیریت کاربران**: مدیریت متمرکز کاربران، امکان جستجو، تغییر نقش و حذف با حفظ یکپارچگی داده‌ها (مدیریت خودکار کلیدهای خارجی هنگام حذف کاربر).

## ۲. مدیریت فضاهای کاری مجزا (Workspace Management)
*   **فضاهای کاری ایزوله (Isolated Workspaces)**: هر فضای کاری دارای تنظیمات، مستندات، اعضا و دیتابیس‌های مختص به خود است. اطلاعات بین فضاهای کاری نشت پیدا نمی‌کند.
*   **شخصی‌سازی رفتار هوش مصنوعی**: امکان تعریف «دستورالعمل سیستمی» (System Prompt) اختصاصی برای هر فضای کاری تا لحن و رفتار مدل بر اساس نیاز آن بخش (مثلاً پشتیبانی، منابع انسانی یا مالی) تنظیم شود.
*   **مدیریت اعضای فضای کاری**: مدیران می‌توانند کاربران موجود در پلتفرم را با ایمیل جستجو کرده و به عنوان کاربر عادی یا مدیر به فضای کاری اضافه کنند.

## ۳. پایگاه دانش و جستجوی معنایی (RAG & Document Processing)
*   **پشتیبانی از فرمت‌های متنوع**: امکان آپلود فایل‌های `PDF`, `DOCX`, `XLSX`, `CSV`, `TXT` و `MD`.
*   **پردازش در پس‌زمینه (Background Processing)**: پردازش فایل‌های حجیم بدون اختلال در تجربه کاربری با استفاده از Background Tasks.
*   **تکه‌تکه کردن (Chunking) و جاسازی (Embedding)**: متون به صورت هوشمند تکه‌تکه شده و توسط مدل‌های Embedding (به صورت پیش‌فرض `BAAI/bge-m3`) به بردارهای عددی تبدیل می‌شوند.
*   **جستجوی برداری (Vector Search)**: ذخیره‌سازی بردارها در پایگاه داده با افزونه `pgvector` که به هوش مصنوعی اجازه می‌دهد دقیق‌ترین و مرتبط‌ترین پاراگراف‌ها را برای پاسخ به کاربر پیدا کند.
*   **رابط کاربری مدرن آپلود**: آپلود همزمان چند فایل، نمایش انیمیشنی وضعیت پردازش فایل‌ها، و مدیریت نام‌های طولانی فایل با انیمیشن Marquee.

## ۴. اتصال هوشمند به پایگاه‌های داده (Database Querying Tool)
*   **پشتیبانی از دیتابیس‌های چندگانه**: امکان اتصال مستقیم به پایگاه‌های داده `PostgreSQL`, `MySQL`, `SQL Server`, `Oracle` و `MongoDB`.
*   **تست اتصال درلحظه**: دکمه اختصاصی جهت بررسی صحت اطلاعات ورود (هاست، پورت، یوزر، پسورد) پیش از ذخیره.
*   **اجرای خودکار کوئری (Autonomous Querying)**: هوش مصنوعی دارای یک «ابزار (Tool)» اختصاصی است که می‌تواند سوال طبیعی کاربر را به زبان SQL یا قالب NoSQL تبدیل کرده، کوئری را روی دیتابیس اجرا نماید و نتایج را تفسیر کند.
*   **محدودیت‌های امنیتی و پایداری**: 
    *   محدودیت تعداد ردیف‌های خروجی (پیش‌فرض ۲۰۰ ردیف)
    *   محدودیت حجم متن پردازشی (حداکثر ۴۰۰۰ کاراکتر)
    *   محدودیت زمانی (Timeout) ۱۵ ثانیه‌ای برای کوئری‌ها
*   **آپلود اسناد راهنمای دیتابیس (Schema Docs)**: کاربر می‌تواند فایل‌هایی آپلود کند که ساختار جداول دیتابیس را به هوش مصنوعی آموزش دهد تا کوئری‌های دقیق‌تری نوشته شود.

## ۵. چت هوشمند و تعاملی (AI Chat Interface)
*   **پاسخ‌گویی جریانی (Streaming Answers)**: نمایش کلمه‌به‌کلمه پاسخ‌های هوش مصنوعی برای تجربه کاربری روان (مشابه ChatGPT).
*   **حفظ تاریخچه مکالمات (Threads)**: دسته‌بندی و حفظ سوابق گفتگوهای گذشته کاربر در هر فضای کاری.
*   **ارتباط با مدل‌های پیشرفته**: امکان اتصال به هر API استاندارد OpenAI (شامل مدل‌های لوکال مثل vLLM، Ollama یا مدل‌های ابری).
*   **پاسخ‌گویی مبتنی بر ترکیب (Hybrid Answering)**: توانایی هوش مصنوعی در تصمیم‌گیری برای اینکه جواب کاربر را از درون «متن فایل‌های آپلودی» بخواند یا برای رسیدن به جواب از «دیتابیس متصل شده» کوئری بگیرد.

## ۶. رابط کاربری پیشرفته و مدرن (UI/UX)
*   **طراحی واکنش‌گرا (Responsive)**: کارکرد صحیح در نمایشگرهای مختلف.
*   **جزئیات بصری (Micro-interactions)**: استفاده از افکت‌های شناور (Hover)، انیمیشن‌های روان، Tooltip، المان‌های شیشه‌ای و وضعیت‌های بارگذاری (Spinners) در تمام بخش‌ها.
*   **مدیریت وضعیت با Zustand**: به‌روزرسانی درلحظه رابط کاربری بدون نیاز به رفرش صفحه.
