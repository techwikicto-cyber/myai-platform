# پلتفرم بینا (Bina Platform) — روند توسعه و تغییرات

پلتفرم چت با اسناد و دیتابیس (مشابه AnythingLLM، بدون Agent/Skill) — FastAPI + React + Postgres/pgvector — اجرا با Docker Compose.

این فایل با هر تغییر مهم به‌روز می‌شود. جدیدترین تغییرات بالاتر است.

---

## ۱۴۰۵/۰۴/۱۵ (۲) — embedding لوکال، فونت ایران‌یکان، پردازش پس‌زمینه اسناد

### سرویس Embedding — ایمیج لوکال شما
- سرویس `embedding` در docker-compose به **`beloved70020/bge-m3:latest`** تغییر کرد (API سازگار OpenAI روی `/v1/embeddings`، پورت داخلی 8080).
- پورت هاست: **8006** (`EMBEDDING_PORT` در `.env`).
- پیش‌فرض‌های backend: `http://embedding:8080/v1` / نوع `openai` / مدل `BAAI/bge-m3`.
- **مهم:** اگر قبلاً در تنظیمات مدل‌ها مقدار قدیمی (`http://embedding:80` با نوع TEI) را ذخیره کرده‌اید، در پنل ادمین آن را به مقادیر جدید تغییر دهید و ذخیره کنید.
- اگر سرور embedding فقط ورودی تکی بپذیرد (نه لیست)، backend خودکار به ارسال تک‌به‌تک سوییچ می‌کند.

### رفع باگ مهم فونت در build
- فونت‌ها در build نهایی (production) اصلاً emit نمی‌شدند و مرورگر فونت سیستم (Tahoma) نشان می‌داد — دلیل اصلی «زشتی فونت‌ها» روی سرور. ایمپورت فونت به `main.tsx` منتقل شد تا Vite فایل‌ها را در build قرار دهد.

### فونت و انیمیشن
- فونت **ایران‌یکان** (وزن‌های کامل، اعداد فارسی) باندل‌شده و آفلاین؛ وزیرمتن به‌عنوان fallback.
- انیمیشن‌ها: ورود نرم پیام‌ها و کارت‌ها (fade-up)، **typing indicator** سه‌نقطه هنگام فکر کردن مدل، بلاب‌های گرادیانی متحرک در صفحه ورود، افکت فشردن دکمه‌ها، pulse روی وضعیت «در حال پردازش».

### پردازش پس‌زمینه اسناد + آگاهی سیستم از تغییرات
- آپلود سند **بلافاصله** پاسخ می‌دهد (وضعیت «در صف») و پارس/embedding در پس‌زمینه انجام می‌شود — دیگر درخواست آپلود برای فایل‌های بزرگ time-out نمی‌شود.
- UI هر ۳ ثانیه وضعیت اسناد در حال پردازش را به‌روز می‌کند تا خودکار «آماده» شوند.
- **آپلود چند فایل همزمان** ممکن شد. هر سند جدیدی در هر زمانی اضافه شود، بلافاصله بعد از «آماده» شدن وارد جستجوی چت می‌شود (چت همیشه روی همه اسناد ready ورک‌اسپیس جستجو می‌کند).

---

## ۱۴۰۵/۰۴/۱۵ — بازطراحی UI و رفع باگ‌های تنظیمات مدل

### رفع باگ‌ها
- **تست اتصال بدون نیاز به ذخیره**: دکمه‌های «تست اتصال» در تنظیمات مدل‌ها حالا *مقادیر فعلی فرم* را تست می‌کنند، نه مقادیر ذخیره‌شده. دیگر لازم نیست اول ذخیره کنید.
- **نمایش مقادیر پیش‌فرض embedding**: فیلدهای Embedding به‌جای placeholder خالی، مقدار مؤثر (`http://embedding:80`، `tei`، `bge-m3`) را نشان می‌دهند.
- **پیام خطای مفهوم برای embedding**: به‌جای «All connection attempts failed»، پیام فارسی با راهنمایی (`docker compose logs -f embedding`) نمایش داده می‌شود. علت رایج: کانتینر embedding بار اول باید مدل bge-m3 را دانلود کند و چند دقیقه طول می‌کشد.
- **حذف warning مربوط به bcrypt** در لاگ بک‌اند (پین کردن `bcrypt==4.0.1`).

### بازطراحی UI (الهام از shadcn/ui)
- **فونت وزیرمتن** به‌صورت باندل‌شده (پکیج npm — بدون نیاز به اینترنت در سرور).
- **تم روشن با سایدبار تیره سبز-زمردی** و رنگ اصلی سبز (emerald) — توکن‌های رنگی CSS variables.
- **کامپوننت‌های یکدست**: Button، Input، Select، Card، Badge، Alert، Spinner + مجموعه آیکون SVG.
- بازطراحی همه صفحات: ورود/راه‌اندازی، چت (حباب‌ها، آواتار، composer جدید)، پنل مدیریت (کاربران + تنظیمات مدل‌ها)، تنظیمات ورک‌اسپیس (عمومی/مستندات/دیتابیس).
- خطاهای پردازش اسناد حالا در پایین لیست مستندات با پیام کامل نمایش داده می‌شوند.

---

## ۱۴۰۵/۰۴/۱۴ — نسخه اول کامل (۵ مرحله)

### مرحله ۱ — اسکلت پروژه
- ساختار `backend/` (FastAPI + SQLAlchemy async + Alembic) و `frontend/` (React + Vite + TypeScript + Tailwind)
- `docker-compose.yml` با ۴ سرویس: postgres (pgvector)، backend، frontend (nginx)، embedding (TEI/bge-m3)
- احراز هویت JWT + سه سطح دسترسی: **admin** (همه‌کاره)، **manager** (ادمین ورک‌اسپیس)، **user** (فقط چت)
- صفحه راه‌اندازی اولیه (ساخت اکانت ادمین در اولین اجرا)

### مرحله ۲ — ورک‌اسپیس و چت
- CRUD ورک‌اسپیس + مدیریت اعضا، Thread و Message
- چت استریم (SSE) با هر LLM سازگار با OpenAI API
- رندر Markdown (جدول/کد/لیست) در پاسخ‌ها
- تنظیمات مدل LLM و Embedding در پنل ادمین با دکمه تست

### مرحله ۳ — اسناد و RAG
- آپلود pdf / docx / xlsx / csv / txt / md → استخراج متن، تکه‌تکه‌سازی (chunking)، embedding، ذخیره در pgvector
- در هر پیام چت، مرتبط‌ترین تکه‌های اسناد همان ورک‌اسپیس به context مدل اضافه می‌شود

### مرحله ۴ — اتصال دیتابیس و کوئری زبان طبیعی
- اتصال به **PostgreSQL / MySQL / MSSQL / Oracle / MongoDB** در سطح ورک‌اسپیس
- تست اتصال + استخراج خودکار اسکیما (جدول‌ها/ستون‌ها؛ برای Mongo: نمونه‌برداری از فیلدها)
- آپلود «سند آموزش اسکیما» برای هر اتصال (معنی جدول‌ها/فیلدها)
- ابزار `query_database` با function calling: مدل کوئری می‌سازد، بک‌اند **فقط SELECT/find** را با سقف ۲۰۰ ردیف و timeout اجرا می‌کند (INSERT/UPDATE/DELETE/DDL و `$out`/`$merge` رد می‌شوند)

### مرحله ۵ — حافظه و نهایی‌سازی
- خلاصه‌سازی خودکار تاریخچه‌های طولانی در `memory_summary` هر گفتگو
- Dockerfile فرانت (build چندمرحله‌ای + nginx با پشتیبانی SSE)
- تست کامل RBAC برای هر سه نقش

---

## نکات دیپلوی

```bash
cp .env.example .env      # مقادیر JWT_SECRET و ENCRYPTION_KEY و POSTGRES_PASSWORD را عوض کنید
docker compose up --build -d
# → http://<server>:3000
```

- بار اول، سرویس embedding مدل bge-m3 را دانلود می‌کند (چند دقیقه؛ با `docker compose logs -f embedding` پیگیری کنید). تا قبل از آماده شدن آن، آپلود سند خطای اتصال می‌دهد.
- برای production حتماً پشت reverse proxy با HTTPS قرار دهید.
- برای اتصال دیتابیس، توصیه می‌شود یک یوزر read-only بسازید.

## کارهای بعدی (Backlog)
- [ ] مدیریت چند Thread در هر ورک‌اسپیس از UI (فعلاً هر کاربر یک گفتگوی پیوسته دارد)
- [ ] مدیریت اعضای ورک‌اسپیس از UI (بک‌اند آماده است)
- [ ] Code-splitting فرانت برای کاهش حجم باندل
