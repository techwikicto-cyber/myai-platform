.env
__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
build/
*.log
.DS_Store

# Host-specific Docker overrides (e.g. custom DNS). Auto-loaded by `docker compose`,
# kept out of git so the tracked docker-compose.yml never conflicts on pull.
docker-compose.override.yml
