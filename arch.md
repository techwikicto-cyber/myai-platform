# معماری سیستم پلتفرم بینا (Bina Platform Architecture)

این سند برای تشریح ساختار کلی و معماری فنی سیستم، با در نظر گرفتن تمام تغییرات و بروزرسانی‌های اخیر تدوین شده است.

## ۱. ساختار کلی (Tech Stack)
این پلتفرم از یک معماری دو-لایه (Frontend / Backend) بهره می‌برد که در محیط Docker با استفاده از Docker Compose پیاده‌سازی شده است.
- **بک‌اند (Backend):** Python 3.11, FastAPI, SQLAlchemy (Async), PostgreSQL (pgvector).
- **فرانت‌اند (Frontend):** React 18, Vite, TypeScript, TailwindCSS, Zustand (برای مدیریت استیت).
- **سرویس‌های جانبی:** سرویس Embedding محلی (BGE-M3) برای تبدیل متن به وکتور بدون نیاز به اینترنت.

## ۲. احراز هویت و دسترسی‌ها (RBAC)
سیستم از JWT برای احراز هویت استفاده می‌کند. کاربران دارای ۳ سطح دسترسی اصلی هستند:
- **Admin (ادمین کل):** دسترسی به تمام بخش‌ها، ساخت کاربر، تعریف مدل‌های زبانی پایه، حذف تمام ورک‌اسپیس‌ها.
- **Manager (مدیر ورک‌اسپیس):** امکان ساخت فضاهای کاری جدید، مدیریت اعضا و دسترسی‌ها در فضاهایی که عضو آن است.
- **User (کاربر عادی):** فقط استفاده از فضاهای کاری که در آن عضو شده است.

**نکته امنیتی جدید:** کاربرانی که توسط ادمین ساخته می‌شوند، در دیتابیس فلگ `must_change_password=True` می‌گیرند. در فرانت‌اند، با استفاده از Global Route Guard در سطح `Layout.tsx`، سیستم کاملاً قفل شده و کاربر در اولین ورود مجبور به تغییر رمز عبور خود است.

## ۳. سیستم پروفایل و آواتار
سیستم دارای یک ماژول پروفایل پیشرفته است:
- کاربران می‌توانند برای خود نام (`full_name`) و عکس پروفایل تعیین کنند.
- **بهینه‌سازی تصاویر:** به جای درگیر کردن سرور با فایل‌های سنگین، فرانت‌اند با استفاده از `Canvas` HTML5، حجم عکس‌ها را در سمت کلاینت به طور خودکار به حداکثر عرض/ارتفاع 200px برش داده، فرمت را به `WebP` تبدیل می‌کند و پس از فشرده‌سازی، آن را به عنوان رشته `Base64` به بک‌اند می‌فرستد. این روش باعث می‌شود هر عکس فقط حدود ۱۰ الی ۲۰ کیلوبایت در ستون `Text` دیتابیس فضا اشغال کند.

## ۴. مدیریت وضعیت چت (State Management)
در بروزرسانی‌های اخیر، وضعیت چت‌ها (پیام‌ها، پیام‌های در حال تایپ، خطاها) از حالت محلی (Local State در صفحات) به حالت سراسری (Global Store) در **Zustand** (`useChatStore` و `useThreadStore`) منتقل شد.
- **مزیت:** اگر کاربر هنگام دریافت پاسخ از مدل زبانی، صفحه را عوض کند (مثلاً به تنظیمات برود) و سپس به چت برگردد، هیچ اطلاعاتی گم نمی‌شود و وضعیت به صورت Real-time همچنان نمایش داده می‌شود. استریم در پس‌زمینه توسط کلاینت ادامه می‌یابد.

## ۵. تعامل با مدل‌های زبانی (LLM) و Function Calling
ارتباط با مدل زبانی (مانند OpenAI یا مدل‌های متن‌باز پشتیبانی شده) از طریق Prompt Injection و ابزارهای Function Calling انجام می‌شود.
- **سیستم‌پرامپت پویا:** نقش‌ها، قوانین ایمنی و پرامپت‌های سفارشیِ هر فضای کاری با هم ترکیب شده و به مدل ارسال می‌شوند.
- **ابزار Database Query:** مدل به صورت هوشمند کوئری‌های SQL تولید می‌کند. بک‌اند این کوئری‌ها را در یک محیط محدود (فقط شامل دستورات `SELECT`) اجرا کرده و نتیجه را برای تولید پاسخ نهایی به مدل برمی‌گرداند.
- **نمایش کوئری‌ها:** برای شفافیت کامل، به سیستم دستور داده شده است تا در صورت تولید کوئری دیتابیس، عین دستورات SQL یا NoSQL اجرا شده را در خروجی Markdown برای کاربر چاپ کند.

## ۶. پردازش اسناد و RAG (Retrieval-Augmented Generation)
- اسناد آپلود شده بلافاصله وضعیت `در صف` می‌گیرند تا کاربر با خطای Time-out مواجه نشود.
- در پس‌زمینه (Background Task)، متن استخراج شده و به Chunkهای متنی تبدیل و توسط سرویس Embedding محلی به Vector تبدیل می‌شود.
- وکتورها در `pgvector` ذخیره شده و در حین چت، با استفاده از Cosine Similarity، مرتبط‌ترین بخش‌ها استخراج شده و به عنوان Context به پرامپت مدل متصل می‌شوند.

## ۷. دیتابیس و مایگریشن (Alembic)
تغییرات دیتابیس به وسیله `alembic` مدیریت می‌شود. اسکریپت `entrypoint.sh` در کانتینر بک‌اند تضمین می‌کند که به محض راه‌اندازی با Docker Compose، جدیدترین تغییرات شمای دیتابیس (مثل اضافه شدن ستون‌های مربوط به پروفایل) به طور خودکار با دستور `alembic upgrade head` اِعمال شود.
