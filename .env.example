# Postgres (metadata + vector store)
POSTGRES_USER=myai
POSTGRES_PASSWORD=change-me
POSTGRES_DB=myai
POSTGRES_PORT=5432

# Backend
JWT_SECRET=change-me-to-a-long-random-string
ENCRYPTION_KEY=change-me-to-a-long-random-string
BACKEND_PORT=8000

# Embedding service (local beloved70020/bge-m3 image, OpenAI-compatible /v1/embeddings on internal port 8080)
EMBEDDING_PORT=8006

# Frontend
FRONTEND_PORT=3000

